#!/usr/bin/env bash
# .xprofile

# Disable touchpad while typing
if [[ -x "$(command -v syndaemon)" ]] ; then {
        syndaemon -d -i 0.5 -m 100 -K
} fi

source "$HOME/bin/monitors.bash"
source "$HOME/bin/wal.sh"
